# 开发日志

## 2026-06-12

### 本轮目标

把原来的本地手机原型，推进成可以给朋友内部使用的网页应用雏形。系统仍然只使用不可提现的虚拟积分，不涉及真实金钱、充值、提现或兑换。

### 后端改动

- 将数据存储从 `data.json` 升级为 SQLite：`mockgaming.sqlite3`。
- 新增用户、会话、比赛、投注、投注明细、积分流水、管理员操作日志等数据表。
- 新增密码哈希存储，不再明文保存密码。
- 新增 Bearer token 登录会话。
- 新增管理员账号和权限校验。
- 普通用户下注改为服务端校验：
  - 前端只提交比赛 ID、投注选项和积分。
  - 后端根据数据库中的比赛与赔率计算总赔率和预计返奖。
  - 防止前端伪造赔率、返奖或比赛内容。
- 新增管理员接口：
  - 创建用户。
  - 加 / 扣积分。
  - 停用 / 启用账号。
  - 重置密码。
  - 手动锁盘 / 解锁。
  - 手动修改赔率。
  - 手动录入比分。
  - 根据比分自动判断主胜 / 平局 / 客胜并结算。
- 新增开赛时间自动锁盘任务：
  - 服务启动后每 60 秒扫描一次。
  - 比赛到达 `kickoff_ts` 后自动锁盘。
  - 除自动锁盘外，不做自动赛程、赔率、赛果同步。

### 前端改动

- 去掉原来的 iOS / Android 手机壳预览和 Tweaks 面板。
- 将应用改成正常 Web App：
  - 桌面端左侧导航。
  - 移动端底部导航。
- 重做登录页，改成更克制、留白更清晰的网页登录界面。
- 新增管理员后台页面 `wc-admin.jsx`。
- 管理员后台支持：
  - 账号管理。
  - 积分调整。
  - 密码重置。
  - 官方赛果页面跳转。
  - 赔率修改。
  - 比分录入和结算。
  - 投注记录、积分流水和操作日志查看。
- 普通用户页面支持：
  - 使用后台最新赔率展示和计算。
  - 查看投注状态。
  - 查看积分流水。
  - 修改密码。

### 官方数据处理决策

- 尝试访问中国体育彩票 / 竞彩网官方页面时，请求被 Tencent Cloud EdgeOne 安全策略拦截。
- 因此本轮不做自动爬取赛程、赔率或赛果。
- 当前方案调整为：
  - 管理员手动维护赛程、赔率和比分。
  - 系统只自动处理到点封盘。
  - 管理后台提供官方页面跳转，方便管理员人工核对赛果。

### 验证情况

- `server.py` 编译通过。
- 核心 JSX 文件解析通过。
- 本地服务入口返回 `200`。
- `/api/state` 返回 `200`。
- 验证过管理员改赔率后，普通用户下注使用服务端新赔率计算。
- 验证过录入比分后，系统自动判断赛果并结算返奖。
- 验证过已到开赛时间的比赛会自动锁盘。

### 默认账号

- 普通用户：`demo / 123456`
- 管理员：`admin / admin123456`

首次部署后应立即修改管理员密码。

### 后续注意事项

- 部署时需要备份 `mockgaming.sqlite3`。
- GitHub Pages 不能运行 Python 后端，不能直接用于正式部署。
- 上线应配置 HTTPS。
- 建议限制后台访问，并使用强密码。
- 如果后续要新增淘汰赛，需要同步更新前端 `wc-data.js` 和后端 `server.py` 中的比赛数据。
